const connection_store = [];
export function closeConnection(name) {
    const conn = connection_store.find((conn) => conn.name === name);
    if (!conn) {
        console.warn(`Connection with name ${name} not found.`);
        return;
    }
    conn.close();
    conn.closed = true;
}
//# sourceMappingURL=socket.js.map